# Pinned to the latest stable versions as of 2026-06-11, all verified together:
# `pytest -q` (80 tests) + `python -m evals.run` (gated scorecard) pass on this set
# under Python 3.13.

# ── Core API ─────────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
sse-starlette==3.4.4
pydantic==2.13.4
pydantic-settings==2.14.1

# ── Agents / LLM ──────────────────────────────────────────
langgraph==1.2.4

# Model providers are plug-and-play and imported lazily — the system runs
# (mock mode, tests, and any one provider) even if the others aren't installed.
# Install only what you'll use; all three are pinned here for a full image.
anthropic==0.105.2      # Anthropic · Claude
google-genai==2.8.0     # Google · Gemini  (the unified `from google import genai` SDK)
openai==2.41.0          # OpenAI · GPT

# ── Structured data + schema-drift hardening (Bridgekeeper layer) ─────
pandas==3.0.3
pandera==0.31.1

# ── Real-document ingestion ─────────────────────────────────
# Text-layer probe + extraction for PDF uploads (pure-Python, BSD). Scanned
# PDFs/images go to the configured provider as native vision attachments.
pypdf==6.12.0

# ── Fuzzy reconciliation ──────────────────────────────────
rapidfuzz==3.14.5

# ── Observability (optional, gated by env) ─────────────────────
# Langfuse v4 is OpenTelemetry-backed; it pulls the OTel SDK transitively.
langfuse==4.7.1

# ── Tests ───────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.4.0
