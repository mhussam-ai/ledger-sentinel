# ── Core API ──────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
sse-starlette==2.1.3
pydantic==2.10.4
pydantic-settings==2.7.0

# ── Agents / LLM ─────────────────────────
langgraph==0.2.60

# Model providers are plug-and-play and imported lazily — the system runs
# (mock mode, tests, and any one provider) even if the others aren't installed.
# Install only what you'll use; all three are pinned here for a full image.
anthropic==0.105.2      # Anthropic · Claude
google-genai==2.8.0     # Google · Gemini  (the unified `from google import genai` SDK)
openai==2.41.0          # OpenAI · GPT

# ── Structured data + schema-drift hardening (Bridgekeeper layer) ─────
pandas==2.2.3
pandera==0.22.1

# ── Fuzzy reconciliation ─────────────────────────
rapidfuzz==3.10.1

# ── Observability (optional, gated by env) ─────────────────────
langfuse==2.57.0

# ── Tests ─────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
